/* Optimized production build generated by CommandKit */
import {
  emojis,
  main
} from "./chunk-GSB7PNNP.js";
import {
  __name
} from "./chunk-4HQ2LG3N.js";

// src/utils/sendErrorReport.ts
import { EmbedBuilder } from "discord.js";
async function sendErrorReport({ description, error, interaction }) {
  const SError = error.toString();
  await interaction.editReply(`${emojis.red_cross} There was an error while executing this command. It has been sent to the bot devs.`).catch(async () => {
    await interaction.reply({ ephemeral: true, content: `${emojis.red_cross} There was an error while executing this command. It has been sent to the bot devs.` }).catch(() => null);
  });
  const errorReportsChannel = await interaction.guild?.channels.fetch(main.errorReportsChannel);
  if (!errorReportsChannel?.isTextBased())
    return;
  let embed = new EmbedBuilder({
    title: "New Error!",
    description: `There was an error!
Error description: ${description}
Error: 
` + error
  }).setColor("#ff0000");
  await errorReportsChannel.send({ content: "@everyone", embeds: [embed] });
}
__name(sendErrorReport, "sendErrorReport");

export {
  sendErrorReport
};
