/* Optimized production build generated by CommandKit */
import {
  __name
} from "./chunk-4HQ2LG3N.js";

// src/subcommands/partner/src/questions.ts
import { EmbedBuilder } from "discord.js";
async function questions_default({ client, interaction }) {
  await interaction.deferReply({ ephemeral: true });
  const user = interaction.options.get("user")?.value;
  const channel = interaction.channel;
  const embed = new EmbedBuilder().setTitle("Partnership questions").setColor("#41cd50").setDescription(
    "**Answer these questions, don't ping staff members to get responce**"
  ).addFields(
    {
      name: "Reason of partnership.",
      value: "Why do you want to partner with us?"
    },
    {
      name: "Your rank.",
      value: "What is your rank?"
    },
    {
      name: "Members without bots.",
      value: "How much members does your server have? (without bots)"
    },
    {
      name: "Any nukes/raids in the past 6 months.",
      value: "How many nukes/raids did your server have in the past 6 months?"
    },
    {
      name: "Server ad",
      value: "Send your server's ad **in separate message**."
    }
  ).setFooter({ text: `Sent by: ${interaction.user.username}` });
  const ping = user ? `||<@!${user}>||` : "";
  await channel.send({
    content: ping,
    embeds: [embed]
  });
  await interaction.editReply("\u2705 Success!");
}
__name(questions_default, "default");

export {
  questions_default
};
