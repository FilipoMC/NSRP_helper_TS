/* Optimized production build generated by CommandKit */
import {
  __name
} from "../chunk-4HQ2LG3N.js";

// src/validations/globalDevOnly.ts
function globalDevOnly_default({ interaction, commandObj, handler }) {
  if (!commandObj?.options?.globalDevOnly)
    return;
  if (!interaction.isChatInputCommand())
    return;
  if (!interaction.inCachedGuild())
    return;
  if (!handler.devUserIds.includes(interaction.user.id)) {
    interaction.reply({ content: "\u274C This command can only be used by developers.", ephemeral: true });
    return true;
  }
}
__name(globalDevOnly_default, "default");
export {
  globalDevOnly_default as default
};
