/* Optimized production build generated by CommandKit */
import {
  __name
} from "../chunk-4HQ2LG3N.js";

// src/validations/devOnlySubcommands.ts
function devOnlySubcommands_default({ client, commandObj, interaction, handler }) {
  const option = commandObj.options?.devOnlySubcommands;
  const interaction1 = interaction;
  if (!option)
    return false;
  if (option.includes(interaction1.options.getSubcommand())) {
    if (!handler.devUserIds.includes(interaction.user.id)) {
      interaction1.reply({ content: "\u274C This command can only be used by developers.", ephemeral: true });
      return true;
    }
  }
}
__name(devOnlySubcommands_default, "default");
export {
  devOnlySubcommands_default as default
};
