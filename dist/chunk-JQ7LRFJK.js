/* Optimized production build generated by CommandKit */
import {
  new_ticket_default
} from "./chunk-NSIRQ52E.js";
import {
  __name
} from "./chunk-4HQ2LG3N.js";

// src/api/index.ts
import express from "express";
import bodyParser from "body-parser";
var PORT = process.env.EXPRESS_PORT;
var TICKETS_TOKEN = process.env.EXPRESS_TICKETS_TOKEN;
async function api_default(client) {
  const app = express();
  app.use(bodyParser.json());
  new_ticket_default(client, app);
  app.listen(PORT, () => {
    console.log(`API server running on port ${PORT}`);
  }).on("error", (error) => {
    console.log(`Error with the API server:`);
    console.log(error);
  });
}
__name(api_default, "default");

export {
  api_default
};
